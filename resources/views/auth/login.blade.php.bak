<x-guest-layout>
    <form method="POST" action="{{ route('login') }}">
        @csrf

        <!-- Email Address -->
        <div>
            <x-input-label for="email" :value="__('Email')" />
            <x-text-input 
                id="email" 
                class="block mt-1 w-full" 
                type="email" 
                name="email" 
                :value="old('email')" 
                required 
                autofocus 
                autocomplete="username" 
            />
            <x-input-error :messages="$errors->get('email')" class="mt-2" />
        </div>

        <!-- Password -->
        <div class="mt-4">
            <x-input-label for="password" :value="__('Password')" />
            <x-text-input 
                id="password" 
                class="block mt-1 w-full" 
                type="password" 
                name="password" 
                required 
                autocomplete="current-password" 
            />
            <x-input-error :messages="$errors->get('password')" class="mt-2" />
        </div>

        <!-- Terms and Policy Checkbox -->
        <div class="mt-4">
            <label for="terms_checkbox" class="inline-flex items-center">
                <input id="terms_checkbox" type="checkbox" class="rounded border-gray-300 text-indigo-600 shadow-sm focus:ring-indigo-500" name="terms_accepted">
                <span class="ms-2 text-sm text-gray-600">
                    I accept Terms and Policy 
                    <a href="#" class="text-indigo-600 hover:text-indigo-900 font-bold" data-bs-toggle="modal" data-bs-target="#termsModal">[Read]</a>
                </span>
            </label>
            <div id="terms_error" class="text-sm text-red-600 mt-2 hidden">
                You must accept Terms & Policy to continue
            </div>
        </div>

        <!-- Buttons Section -->
        <div class="flex items-center justify-between mt-4">
            <!-- Forgot Password Left -->
            @if (Route::has('password.request'))
                <a class="underline text-sm text-gray-600 hover:text-gray-900 rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" href="{{ route('password.request') }}">
                    {{ __('Forgot your password?') }}
                </a>
            @endif

            <!-- Social Icons + Login Button Right -->
            <div class="flex items-center gap-3">
                <div class="flex gap-2">
                    <!-- Google Login Icon -->
                    <a href="{{ url('/auth/google') }}" class="inline-flex items-center justify-center w-[38px] h-[38px] bg-white border border-gray-300 rounded-md transition duration-150 ease-in-out shadow-sm hover:bg-gray-50" title="Login with Google">
                        <svg width="20" height="20" viewBox="0 0 24 24">
                            <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
                            <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
                            <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z" fill="#FBBC05"/>
                            <path d="M12 5.38c1.62 0 3.06.56 4.21 1.66l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
                        </svg>
                    </a>

                    <!-- Facebook Login Icon -->
                    <a href="{{ url('/auth/facebook') }}" class="inline-flex items-center justify-center w-[38px] h-[38px] bg-white border border-gray-300 rounded-md transition duration-150 ease-in-out shadow-sm hover:bg-gray-50" title="Login with Facebook">
                        <i class="fab fa-facebook" style="color: #1877F2; font-size: 1.25rem;"></i>
                    </a>
                </div>

                <x-primary-button>
                    {{ __('Log in') }}
                </x-primary-button>
            </div>
        </div>

        <!-- "Don't have an account?" line below buttons -->
        <div class="mt-4 text-center text-sm text-gray-600">
            Don't have an account? 
            <a href="{{ route('register') }}" class="underline text-indigo-600 hover:text-indigo-900">
                Register
            </a>
        </div>
    </form>

    <script>
        document.querySelector('form').addEventListener('submit', function (e) {
            const checkbox = document.getElementById('terms_checkbox');
            const errorDiv = document.getElementById('terms_error');
            
            if (!checkbox.checked) {
                e.preventDefault();
                errorDiv.classList.remove('hidden');
            } else {
                errorDiv.classList.add('hidden');
            }
        });
    </script>
</x-guest-layout>
